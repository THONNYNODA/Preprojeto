import { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle `

    *{
        margin: 0;
        padding: 0;
        box-size: border-box;
    };

    html,body{
        width: 100%;
        height:100%;
        // //background-image: linear-gradient(to top, #3eb873, #53ca94, #6cdcb3, #87eed0, #a6ffec);
        
    }

`